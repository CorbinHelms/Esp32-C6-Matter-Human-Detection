# Partition table for the Seeed XIAO ESP32-C6 (4 MB flash).
#
# NOTE: this board has 4 MB of flash (verified on hardware via espflash chip
# detection), not 8 MB. The whole usable region past the bootloader/partition
# table/NVS/phy is ~3.68 MB, so the factory app is sized to fill it.
#
# The Matter+Thread firmware is ~1.7 MB, far larger than the default 1 MB app
# partition, so we define a ~3.68 MB factory app (0x3B0000 = 0x400000 - 0x50000)
# which leaves ample headroom. The NVS partition is 256 KB to give the Matter
# fabric/ACL key-value store room (the first NVS partition is what
# `matter::persistent_store` uses).
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x40000,
phy_init,   data, phy,     0x49000,  0x1000,
factory,    app,  factory, 0x50000,  0x3B0000,
